#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *  * _strlen - returns the length of a string.
 *   * @s: The string to print.
 *    *
 *     * Return: The length of a string.
 *      */
int _strlen(char *s)
{
	return (strlen(s));
}

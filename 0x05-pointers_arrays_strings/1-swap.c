#include "main.h"
#include <stdio.h>

/**
 * swap_int - Swaps the values of two intgers.
 * @a: Intger to be swaped.
 * @b: Other intger to be swaped.
 */
void swap_int(int *a, int *b)
{
	int tmp = *a;
	*a = *b;
	*b = temp;
}
